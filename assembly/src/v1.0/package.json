{
  "name": "assembler",
  "version": "1.0.0",
  "description": "ANC16 std 2 assembler",
  "main": "index.js",
  "scripts": {
    "build": "tsc --build",
    "start": "tsc --build && cls && echo ------------------------ && node index.js ./test/hello_world.anc16 -h --h-symbol-ref --h-draw",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Simone Ancona",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^18.13.0",
    "colors": "^1.4.0",
    "fs": "^0.0.1-security",
    "nexe": "^4.0.0-rc.2",
    "typescript": "^4.9.5"
  }
}
