type Labels = Array<{ name: string, address: number }>

export function disassemble(buffer: Uint8Array, options: { useHeader: boolean, comments: boolean }): string {
	let out: string = "";
	let labels: Labels = [];
	if (options.useHeader)

		return out;
}

function handleHeader(buffer: Uint8Array, labels: Labels) {

}